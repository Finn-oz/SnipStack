# 更新日志

SnipStack 的所有重要变更都会记录在本文件中。

SnipStack 是 [EcoPaste](https://github.com/EcoPasteHub/EcoPaste) 的硬分叉
(2026-08 从上游 v1.1.0 线分叉)。继承的剪贴板管理器代码历史请查阅上游更新日志。

## [Unreleased]

## [0.1.5] - 2026-08-22

### 修复

- 应用内显示的图标现已全部改为 SnipStack 自有图标:剪贴板窗口左上角、
  偏好设置侧栏、引导页、更新窗口,以及无来源应用条目的兜底图标,此前
  仍显示上游 EcoPaste 的 logo——更名时漏换了前端图标资源(窗口、托盘
  与安装包图标本就是正确的)。

## [0.1.4] - 2026-08-22

### 修复

- 安装包现在随 `SnipStack.exe` 一并部署 Microsoft Visual C++ 运行库 DLL。
  此前所有版本在未安装 VC++ Redistributable 的全新 Windows 上都无法启动,
  报「找不到 MSVCP140_1.dll」:OCR 引擎静态链接的 ONNX Runtime 仍依赖 C++
  标准库,0.1.0 的静态 CRT 改动并未覆盖它(0.1.0 日志中「无需 VC++
  运行库即可安装」的说法因此并不准确)。现在不再需要单独安装运行库。

## [0.1.3] - 2026-08-21

### 修复

- 窗口几何不再在 UI 线程上写盘:落盘移至专用后台线程并采用原子写,
  磁盘缓慢、杀毒软件扫描或云同步目录不会再在窗口隐藏时卡住应用——
  这是偶发「托盘图标消失 / 应用无法再次打开」问题的疑似诱因之一。
  退出与存储位置迁移前仍会同步刷盘,数据不会丢失。
- 卡死看门狗细化:系统拖拽被识别为预期内停顿(不再误采集);拖拽期间
  的真实卡死超过升级阈值后仍会取证;每次独立卡死事件至多记录一份
  minidump。

## [0.1.2] - 2026-08-20

### 新增

- 运行期诊断能力,用于排查偶发的「托盘图标消失 / 应用无法再次打开」问题:
  panic 会连同调用栈写入日志;看门狗在主线程无响应时记录时长与 GDI/USER
  句柄计数,并在日志目录旁生成 minidump;正常退出会在日志留下明确标记。
  所有数据仅保存在本地日志目录——SnipStack 依旧不含任何遥测上报。

## [0.1.1] - 2026-08-14

### 新增

- 官网下载页(https://finn-oz.github.io/SnipStack/)。

### 变更

- 首个通过自动更新分发的版本——用于对已安装 v0.1.0 的用户端到端验证更新链路。

## [0.1.0] - 2026-08-14

首个公开版本。Windows 11 x64,NSIS 安装包,暂未签名(首次运行会出现
SmartScreen 提示——参见 README 安装说明)。

### 新增

- 截屏取字:全局热键(默认 `Alt+S`)或托盘菜单唤起每显示器框选覆盖层;选区经
  PP-OCRv5 mobile 离线识别,文本自动进剪贴板,并以**普通文本条目**存入历史
  (可搜索、自动去重、URL/邮箱识别——与你自己复制的文本完全一致)。换行模式
  (保留/合并)与自动复制可在「截屏取字」设置页配置。
- 框选区域内 QR/条码识别:发现码直接取值(多个码按行拼接),未命中回落 OCR;
  可配置开关。
- 可下载 OCR 语言包(韩文、拉丁字母语言、俄语/东斯拉夫、泰文、阿拉伯文,每个
  8-13 MB):应用内下载、进度显示、SHA-256 校验、含备用镜像源;内置模型已覆盖
  简繁中文、英文与日文。所选语言包不可用时识别自动回落内置模型,设置界面注明
  各语言包的文字覆盖范围。
- 复制图片后台 OCR:剪贴板收录的图片在后台识别,凭文字即可全文搜索;截屏取字
  条目与已索引条目不会被重复识别或覆盖。
- 剪贴板监听遵守 Windows 排除格式约定(`ExcludeClipboardContentFromMonitorProcessing`、
  `Clipboard Viewer Ignore`、`CanIncludeInClipboardHistory=0`),KeePass、1Password 等
  密码管理器复制的内容不会被记录;截屏取字结果与复制文本遵循同一套密文采集策略。
- 框选覆盖层聚焦光标所在显示器,多显示器下 Esc 立即可用;剪贴板窗口新增
  截屏取字完成 toast(字数或错误信息)。
- 全新 SnipStack 应用图标(选区括号 + 卡片堆叠 + 文字线)。
- 项目从 EcoPaste 分叉并更名为 SnipStack(标识符 `com.snipstack.app`,
  备份扩展名 `.snipstackbak`,数据目录 `SnipStackData`)。

### 变更

- 剪贴板分组栏点击「全部」现在会清除分类与自定义分组筛选,不再无响应。
- C 运行时改为静态链接:在未安装 VC++ 运行库的干净 Windows 上可直接运行。
- 发布流水线只构建 Windows x64 NSIS 安装包,并在构建时拉取 OCR 模型;
  自动更新端点指向 SnipStack 的 GitHub Releases。

### 已知问题

- 「以管理员运行」(向提权窗口粘贴)在本版本中隐藏:提权链路存在已知缺陷
  (提权重启后引导窗口丢失、开机自启同步失败、跨权限单实例失效),修复后恢复。
